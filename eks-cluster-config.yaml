apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: fitness-tracker-cluster
  region: us-west-1

managedNodeGroups:
  - name: workers
    instanceType: t3.small
    desiredCapacity: 3
    minSize: 3
    maxSize: 3
    volumeSize: 20
    labels:
      role: worker
